## Critical（P0）

無。

## Important（P1）

### P1-2 尚未形成真正的「兩個可貼回覆 round-trip」

證據：

- `parseHintResult(...)` 只收到 `turns`，沒有收到 `difficulty` 或 `scaled`；因此三個難度只是重複執行同一解析：
  ```ts
  const parsed = parseHintResult(JSON.stringify(goodReplies), {
    turns: pr5Turns(2),
  });
  ```
- `buildHintDecision(...)` 沒有使用 `parsed.replies`，而是直接使用原始的 `goodReplies.warmUp`。
- 第二句 `steady` 完全沒有進入 `buildHintDecision`：
  ```ts
  replyType: "warm_up",
  replyText: goodReplies.warmUp,
  ```
- 所以目前只證明：
  1. 同一 JSON 可以被重複解析；
  2. warmUp 在三個 profile 下得到固定 decision。
  
  它沒有證明「兩個解析後的可貼回覆，在三難度下都能走完整決策路徑」。

驗證方式：

- 從 `parsed.replies` 逐一取得兩個回覆，分別送入 `buildHintDecision`。
- 三種難度都要覆蓋 warmUp、steady，並斷言不被拒絕及 decision 結構有效。
- 若原要求還包括實際 LLM 輸出品質，需另以固定模型回應／mock integration 驗證；目前靜態測試只能證明 deterministic guard，不足以驗證產生品質。

這是第一輪 P1-2 的核心證據缺口，因此仍屬阻擋項。

## Minor（P3）

### P3-6 的測試無法擋住註解聲稱的「抄大段改一字」

證據：

```ts
assert(!config.hintStandard.includes(line))
```

這只會攔截完整、逐字相同的一整行。NPC 原句只要改一個字，就不再被 `includes(line)` 命中，與註解「擋『抄大段改一字』的重用」矛盾。

另外，實作是按換行切割：

```ts
for (const rawLine of config.prompt.split("\n"))
```

若同一行包含多個句子，只抄其中一個完整句子，也不會命中整行比對。

驗證方式：

- 加入兩個反例 fixture：
  - 完整抄一個句子，但原 prompt 該行另有其他句子。
  - 抄長句但只改一個字。
- 若需求只禁止「逐字整行重用」，應修正測試名稱與註解；若確實禁止近似重用，需採句界切割及正規化後的長片段／相似度比對。

## Uncertain

### P1-4 的正向斷言尚不能確定字串來自 Game tactic／FSM 證據

證據：

```ts
const gameText = buildHintMessages(...).map(...).join("\n");
assert(gameText.includes("phase:"));
assert(gameText.includes("targetVariable:"));
```

這是在整包串接後的 prompt 搜尋通用欄位名，packet 沒有證據排除它們來自 schema、格式說明或其他區塊。因此它確實補了正向斷言，但未充分證明 Game strategy 區塊本身完整存在。

驗證方式：

- 斷言預期的實際 `phase`、`targetVariable` 值，或比對明確的 Game evidence 區塊。
- 做 mutation check：移除 Game strategy 注入後，該測試必須失敗。

## 看起來正確的部分

- P1-1：`PracticeProfile.hintStandard`、`DifficultyConfig.hintStandard` 與相關使用點已對齊；完整測試通過也降低遺漏舊欄位引用的風險。
- P1-3：注入標籤已加入 `INTERNAL_VISIBLE_LABELS`，且 `parseHintResult` 有對應 `hint_internal_label_leak` 測試。不封鎖「輕鬆／一般／挑戰」等自然語詞的理由合理。
- P1-4：至少已從純負面測試提升為包含正面訊號；剩餘問題是證據歸屬不夠精確。
- P2-5：三難度的要求關鍵詞已逐項覆蓋。
- Uncertain-7：`includeCoachingStandard` 預設為 `false`，只有 hint prompt 明確傳入 `true`；fact ledger 測試亦確認 partner evidence 保留但不含該標籤，隔離方向正確。
- 執行端回報局部 241/0、全套 1644/0，沒有顯示既有測試回歸。

**Verdict：BLOCK**

原因是第一輪 P1-2 所承諾的「兩個可貼回覆完整 round-trip」仍未由新增測試實際證明；其餘主要修正看起來成立，P3 與 Game assertion 則屬較低風險的測試精度問題。
