# Claude review unavailable

No reviewer output was returned.

Two foreground attempts with the default `fable` model timed out with empty
stdout and stderr. A third isolated background attempt using explicit
`-Model sonnet` also remained at zero-byte stdout/stderr after more than one
minute and was stopped together with only its verified descendant processes.

This file is failure evidence, not a completed review.
