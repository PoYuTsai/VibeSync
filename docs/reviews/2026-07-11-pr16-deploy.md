# PR #16 Deploy Report

> Archived from the local gstack deploy report after verification.

- Date: 2026-07-11 (Asia/Taipei)
- Pull request: https://github.com/PoYuTsai/VibeSync/pull/16
- Source: `codex/bruce-ux-followup`
- Target: `main`
- Merge method: squash (auto-merge completed immediately)
- Merge SHA: `cd053be54e8433e13e912da6d1dc4cf95e9746b0`
- Change type: mixed Flutter client, Edge Function, tests, and documentation

## Verification

- PR blocking CI: passed before merge.
- Codex high-risk double review: approved, P0/P1/P2 = 0.
- Edge deploy: passed — https://github.com/PoYuTsai/VibeSync/actions/runs/29151547337
- Web deploy: passed — https://github.com/PoYuTsai/VibeSync/actions/runs/29151547327
- iOS/Android Firebase distribution: passed — https://github.com/PoYuTsai/VibeSync/actions/runs/29151547335
- TestFlight: build 1.0.1 (323) uploaded successfully — https://github.com/PoYuTsai/VibeSync/actions/runs/29150207676
- Web canary: `https://web-beta-tawny.vercel.app` returned HTTP 200.
- Supabase canary: `practice-chat` is ACTIVE at version 79.
- Remote `main`: points to the merge SHA above.

## Non-blocking test debt

The distribution workflow intentionally treats the repository-wide `flutter test` command as non-blocking (`flutter test || echo ...` plus `continue-on-error`). Therefore its green status proves build/upload success, not a green full suite.

- Previous main run: 1988 passed, 13 failed, 3 skipped.
- This merge run: 2038 passed, 14 failed, 3 skipped.
- Local full rerun: 2063 passed, 3 failed, 4 skipped.

The Linux failure-set comparison found one new failure: an old copy-sweep snapshot still expects `還沒有互動紀錄`, while this PR intentionally changed the product copy and its dedicated test to `還沒有對話`. The other 13 failures are unchanged from the previous main run: three Flutter 3.44-only assertions in unchanged analysis bubble code, nine visual-proof tests whose font setup does not support the Ubuntu runner, and one root app smoke test that omits the required notification-provider override. All changed archive, OCR tutorial, draw telemetry, partner, and conversation bundles passed. Independent post-merge audits found P0/P1 product regressions = 0 and classified rollback risk as higher than dogfood risk.

The TestFlight #323 release workflow used the same non-blocking full-suite policy and recorded the same 2038/14/3 result before uploading. Its successful status confirms the upload, not a fully green repository-wide suite.

## Worktree safety

The pre-existing user-owned `pubspec.lock` change was not staged or altered; its diff fingerprint remained `155151b4d3096ddc42a4457638cd8984fb9d8620`. The local feature branch is retained to avoid switching across that dirty file.

## Verdict

DEPLOYED AND VERIFIED
