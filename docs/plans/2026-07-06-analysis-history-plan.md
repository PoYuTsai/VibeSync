# 案2：分析事件歷史表 實作計畫

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 建立本機 Hive 歷史事件表（analyze 熱度＋practice 溫度），讓報告頁畫出「單對象熱度時間序列（真日期 x 軸）」與「練習溫度成長曲線」。

**Architecture:** 新 feature `lib/features/analysis_history/` 完全照抄 `coaching_memory` 三層模式（entity → repository interface → Hive impl → Riverpod provider），兩個寫入 hook 分別掛在 analyze 快照持久化與 practice session 持久化之後（皆 best-effort try-catch 不 rethrow）。報告頁只改熱度趨勢卡資料源＋新增一張練習溫度卡，其餘區塊（averageScore/scoreDelta/comparisons/階段分佈）維持現行 Conversation 邏輯完全不動。

**Tech Stack:** Flutter/Riverpod/Hive(AES)/fl_chart

---

## 執行前必讀（皆已對真實 codebase 查證，2026-07-06）

**設計真相源**：`docs/plans/2026-07-06-analysis-history-design.md`——欄位表、box 名 `analysis_history_events`、typeId 24（entity）/25（enum）、500 筆剪枝、hook best-effort、只在 `temperatureScore != null` 寫 practice 事件，全部照它，一字不差。

**模板三層（新程式碼照抄此模式）**：
- `lib/features/coaching_memory/domain/entities/coaching_outcome_event.dart`
- `lib/features/coaching_memory/domain/repositories/coaching_outcome_repository.dart`
- `lib/features/coaching_memory/data/repositories/coaching_outcome_repository_impl.dart`
- `lib/features/coaching_memory/data/providers/coaching_outcome_providers.dart`
- 測試模板：`test/unit/features/coaching_memory/data/repositories/coaching_outcome_repository_impl_test.dart`（真 Hive box 進 `./.dart_tool/` 暫存目錄）＋ `test/helpers/memory_coaching_outcome_repository.dart`（in-memory fake）

**Hive 產生器事實（重要，決定工作流）**：
- 專案用 `hive_ce` + `hive_ce_generator`（pubspec dev_dependencies 有 `build_runner: ^2.4.13`、`hive_ce_generator: ^1.6.0`）。
- entity 檔頂部用 `part 'xxx.g.dart';` + `@HiveType/@HiveField` 註解，adapter 由 build_runner 產生。
- `lib/hive_registrar.g.dart` **是 build_runner 產物**（檔頭「Generated by Hive CE / Do not modify」）——**絕不手改**，跑 `dart run build_runner build --delete-conflicting-outputs` 會自動把新 adapter 加進去。產出的 registrar 變更要一併 commit（檔頭寫明 Check in to version control）。
- app 執行期**不用** registrar extension——`lib/core/services/storage_service.dart` 是逐行手動 `Hive.registerAdapter(...)`（32-55 行），所以新 adapter 仍要在 storage_service 手動加註冊。

**Hook 現場（行號已核對）**：
- analyze：`lib/features/analysis/presentation/screens/analysis_screen.dart` 的 `_persistLatestAnalysisSnapshot`（1290-1319 行）。呼叫端去重 gate 在 891-896 行（`alreadyPersisted` 比對 `lastAnalyzedMessageCount`＋snapshot JSON，命中即 return）。另一個呼叫點在 3804 行（listener 路徑）。螢幕是 ConsumerStatefulWidget，有 `ref` 可直接 read provider；`Uuid` 已 import（1268 行有用）。
- practice：`lib/features/practice_chat/data/providers/practice_chat_providers.dart` 的 `_persist()`（1249-1277 行），呼叫點 936 / 1068 / 1167（endPractice 在 1143-1176）。**`PracticeChatController` 沒有 Ref**——依賴全走建構子注入（見 288-346 行），hook 的 repository 必須加建構子參數，由 `practiceChatControllerProvider`（1380-1400 行）wire 進去。

**報告頁現場**：
- 服務實際路徑是 `lib/features/report/data/services/report_data_service.dart`（**不是** `lib/services/`）。
- Chart：`lib/features/report/presentation/widgets/heat_trend_chart.dart`（302 行）。
- Providers：`lib/features/report/data/providers/report_providers.dart`。
- 畫面：`lib/features/report/presentation/screens/my_report_screen.dart`（報告區塊有 Free gating；三張圖在付費＋非空分支內）。
- 既有測試：`test/widget/features/report/my_report_screen_test.dart`。**report_data_service 與 heat_trend_chart 目前沒有專屬測試檔**，本計畫新建。

**環境鐵則**：
- 測試指令一律 `flutter test <路徑>`（專案 CI 也是裸 `flutter test`）。
- WSL 的 `dart format` 壞掉——**絕不跑 format**，手寫時自己對齊。
- **絕不 `git add pubspec.lock`**、絕不 `git add -A`；每次 commit 用明確檔案路徑。
- 每個 commit 後立即 push。
- widget 測試如有動畫，`pumpAndSettle` 必須收斂（fl_chart LineChart 的資料切換動畫是有限時長，安全）。
- /mnt/c 寫檔後若行為怪異，先檢查檔案有無 NUL byte（歷史坑）。

---

## Task 1：Entity＋enum＋Hive adapter（typeId 24/25）

**為什麼**：時間序列資料 PracticeSession（同對象覆寫）與 Conversation（只有最新分數）都留不住，需要 append-only 事件表。模板＝`coaching_outcome_event.dart`。

**Files:**
- Create: `lib/features/analysis_history/domain/entities/analysis_history_event.dart`
- Create(generated): `lib/features/analysis_history/domain/entities/analysis_history_event.g.dart`（build_runner 產）
- Modify(generated): `lib/hive_registrar.g.dart`（build_runner 自動更新，絕不手改）
- Test: `test/unit/features/analysis_history/domain/entities/analysis_history_event_test.dart`

### Step 1: 寫失敗測試

```dart
// test/unit/features/analysis_history/domain/entities/analysis_history_event_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';

void main() {
  test('analyze factory 填 analyze 欄位、practice 欄位為 null', () {
    final event = AnalysisHistoryEvent.analyze(
      id: 'e-1',
      createdAt: DateTime.utc(2026, 7, 6),
      conversationId: ' c-1 ',
      subjectName: ' 小雲 ',
      enthusiasmScore: 72,
      gameStageLabel: 'premise',
    );

    expect(event.kind, AnalysisHistoryKind.analyze);
    expect(event.conversationId, 'c-1'); // trim
    expect(event.subjectName, '小雲');
    expect(event.enthusiasmScore, 72);
    expect(event.gameStageLabel, 'premise');
    expect(event.profileId, isNull);
    expect(event.roundIndex, isNull);
    expect(event.temperatureScore, isNull);
  });

  test('practice factory 填 practice 欄位、analyze 欄位為 null', () {
    final event = AnalysisHistoryEvent.practice(
      id: 'e-2',
      createdAt: DateTime.utc(2026, 7, 6, 1),
      profileId: 'practice_girl_007',
      roundIndex: 2,
      temperatureScore: 38,
      familiarityScore: 12,
      relationshipStageLabel: '破冰',
    );

    expect(event.kind, AnalysisHistoryKind.practice);
    expect(event.profileId, 'practice_girl_007');
    expect(event.roundIndex, 2);
    expect(event.temperatureScore, 38);
    expect(event.familiarityScore, 12);
    expect(event.relationshipStageLabel, '破冰');
    expect(event.conversationId, isNull);
    expect(event.enthusiasmScore, isNull);
  });

  test('id 空字串 → ArgumentError', () {
    expect(
      () => AnalysisHistoryEvent.analyze(
        id: '  ',
        createdAt: DateTime.utc(2026, 7, 6),
      ),
      throwsArgumentError,
    );
  });

  test('adapter typeId 鎖定 24/25（設計文件拍板，絕不漂移）', () {
    expect(AnalysisHistoryEventAdapter().typeId, 24);
    expect(AnalysisHistoryKindAdapter().typeId, 25);
  });
}
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/unit/features/analysis_history/domain/entities/analysis_history_event_test.dart
```

預期：**編譯失敗**（`analysis_history_event.dart` 不存在）。編譯錯即紅燈。

### Step 3: 最小實作

寫 entity（欄位編號＝設計文件欄位表的 # 欄，一字不差）：

```dart
// lib/features/analysis_history/domain/entities/analysis_history_event.dart
import 'package:flutter/foundation.dart';
import 'package:hive_ce/hive_ce.dart';

part 'analysis_history_event.g.dart';

/// 案2：本機分析事件歷史（analyze 熱度 / practice 溫度共用一張表）。
/// 本機 only，絕不上傳。append-only，repository 寫入時超過 500 筆刪最舊。
@HiveType(typeId: 25)
enum AnalysisHistoryKind {
  @HiveField(0)
  analyze,
  @HiveField(1)
  practice,
}

@immutable
@HiveType(typeId: 24)
class AnalysisHistoryEvent {
  @HiveField(0)
  final String id;

  @HiveField(1)
  final AnalysisHistoryKind kind;

  /// 事件時間（報告頁 x 軸真日期來源）。
  @HiveField(2)
  final DateTime createdAt;

  /// analyze 用（hook 現場只有 conversationId，沒有 partnerId）。
  @HiveField(3)
  final String? conversationId;

  /// 對象名快照（選擇器顯示用，防改名/刪除後查不到）。
  @HiveField(4)
  final String? subjectName;

  @HiveField(5)
  final int? enthusiasmScore;

  @HiveField(6)
  final String? gameStageLabel;

  /// practice 用（practice_girl_NNN）。
  @HiveField(7)
  final String? profileId;

  /// practice 輪次 1–3。
  @HiveField(8)
  final int? roundIndex;

  @HiveField(9)
  final int? temperatureScore;

  @HiveField(10)
  final int? familiarityScore;

  @HiveField(11)
  final String? relationshipStageLabel;

  /// Hive rebuild 用寬鬆建構子；寫入路徑一律走 [analyze] / [practice] factory。
  const AnalysisHistoryEvent({
    required this.id,
    required this.kind,
    required this.createdAt,
    this.conversationId,
    this.subjectName,
    this.enthusiasmScore,
    this.gameStageLabel,
    this.profileId,
    this.roundIndex,
    this.temperatureScore,
    this.familiarityScore,
    this.relationshipStageLabel,
  });

  factory AnalysisHistoryEvent.analyze({
    required String id,
    required DateTime createdAt,
    String? conversationId,
    String? subjectName,
    int? enthusiasmScore,
    String? gameStageLabel,
  }) {
    return AnalysisHistoryEvent(
      id: _requireId(id),
      kind: AnalysisHistoryKind.analyze,
      createdAt: createdAt,
      conversationId: _optionalTrim(conversationId),
      subjectName: _optionalTrim(subjectName),
      enthusiasmScore: enthusiasmScore,
      gameStageLabel: _optionalTrim(gameStageLabel),
    );
  }

  factory AnalysisHistoryEvent.practice({
    required String id,
    required DateTime createdAt,
    String? profileId,
    int? roundIndex,
    int? temperatureScore,
    int? familiarityScore,
    String? relationshipStageLabel,
  }) {
    return AnalysisHistoryEvent(
      id: _requireId(id),
      kind: AnalysisHistoryKind.practice,
      createdAt: createdAt,
      profileId: _optionalTrim(profileId),
      roundIndex: roundIndex,
      temperatureScore: temperatureScore,
      familiarityScore: familiarityScore,
      relationshipStageLabel: _optionalTrim(relationshipStageLabel),
    );
  }

  static String? normalizeScope(String? value) => _optionalTrim(value);

  static String _requireId(String id) {
    final normalized = id.trim();
    if (normalized.isEmpty) {
      throw ArgumentError('AnalysisHistoryEvent.id must be non-empty');
    }
    return normalized;
  }

  static String? _optionalTrim(String? value) {
    final trimmed = value?.trim();
    return trimmed == null || trimmed.isEmpty ? null : trimmed;
  }
}
```

跑 build_runner 產 adapter（同時自動更新 `lib/hive_registrar.g.dart`）：

```bash
dart run build_runner build --delete-conflicting-outputs
```

確認產物：`lib/features/analysis_history/domain/entities/analysis_history_event.g.dart` 存在且非空、`lib/hive_registrar.g.dart` diff 只多了 `AnalysisHistoryEventAdapter` / `AnalysisHistoryKindAdapter` 兩行（×2 個 extension）與 import。**若 registrar diff 動到其他 adapter 行，停下來查原因。**

### Step 4: 跑測試確認過

```bash
flutter test test/unit/features/analysis_history/domain/entities/analysis_history_event_test.dart
```

預期：4 tests passed。

### Step 5: Commit

```bash
git add lib/features/analysis_history/domain/entities/analysis_history_event.dart \
        lib/features/analysis_history/domain/entities/analysis_history_event.g.dart \
        lib/hive_registrar.g.dart \
        test/unit/features/analysis_history/domain/entities/analysis_history_event_test.dart
git commit -m "案2批1 分析事件歷史 entity＋kind enum（typeId 24/25，build_runner 產 adapter）"
git push
```

---

## Task 2：Repository（append＋500 筆剪枝＋按 kind/conversationId 查詢）

**為什麼**：寫入端要剪枝上限、讀取端要兩種過濾。模板＝`coaching_outcome_repository.dart`＋`coaching_outcome_repository_impl.dart`（排序沿用模板慣例 newest-first；報告頁服務層再轉升序）。

**Files:**
- Create: `lib/features/analysis_history/domain/repositories/analysis_history_repository.dart`
- Create: `lib/features/analysis_history/data/repositories/analysis_history_repository_impl.dart`
- Test: `test/unit/features/analysis_history/data/repositories/analysis_history_repository_impl_test.dart`

### Step 1: 寫失敗測試

照抄 `coaching_outcome_repository_impl_test.dart` 的真 Hive box 模式：

```dart
// test/unit/features/analysis_history/data/repositories/analysis_history_repository_impl_test.dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:hive_ce/hive_ce.dart';
import 'package:vibesync/features/analysis_history/data/repositories/analysis_history_repository_impl.dart';
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';

const _testHivePath = './.dart_tool/test_hive_analysis_history_repo';
const _testBoxName = 'test_analysis_history_events';

AnalysisHistoryEvent _analyzeEvent(
  String id, {
  String? conversationId = 'c-1',
  int? score = 60,
  DateTime? createdAt,
}) =>
    AnalysisHistoryEvent.analyze(
      id: id,
      createdAt: createdAt ?? DateTime.utc(2026, 7, 6),
      conversationId: conversationId,
      subjectName: '小雲',
      enthusiasmScore: score,
      gameStageLabel: 'premise',
    );

AnalysisHistoryEvent _practiceEvent(
  String id, {
  int? temperature = 30,
  DateTime? createdAt,
}) =>
    AnalysisHistoryEvent.practice(
      id: id,
      createdAt: createdAt ?? DateTime.utc(2026, 7, 6),
      profileId: 'practice_girl_001',
      roundIndex: 1,
      temperatureScore: temperature,
    );

void main() {
  setUpAll(() {
    Hive.init(_testHivePath);
    if (!Hive.isAdapterRegistered(24)) {
      Hive.registerAdapter(AnalysisHistoryEventAdapter());
    }
    if (!Hive.isAdapterRegistered(25)) {
      Hive.registerAdapter(AnalysisHistoryKindAdapter());
    }
  });

  late Box<AnalysisHistoryEvent> box;
  late AnalysisHistoryRepositoryImpl repo;

  setUp(() async {
    box = await Hive.openBox<AnalysisHistoryEvent>(_testBoxName);
    repo = AnalysisHistoryRepositoryImpl(box);
  });

  tearDown(() async {
    await box.deleteFromDisk();
  });

  tearDownAll(() async {
    await Hive.close();
    final dir = Directory(_testHivePath);
    if (await dir.exists()) await dir.delete(recursive: true);
  });

  test('append + listRecent 回 newest-first', () async {
    await repo.append(_analyzeEvent('old', createdAt: DateTime.utc(2026, 1)));
    await repo.append(_analyzeEvent('new', createdAt: DateTime.utc(2026, 5)));

    expect(repo.listRecent().map((e) => e.id), ['new', 'old']);
  });

  test('listByKind 只回該 kind', () async {
    await repo.append(_analyzeEvent('a-1'));
    await repo.append(_practiceEvent('p-1'));

    expect(
      repo.listByKind(AnalysisHistoryKind.practice).map((e) => e.id),
      ['p-1'],
    );
    expect(
      repo.listByKind(AnalysisHistoryKind.analyze).map((e) => e.id),
      ['a-1'],
    );
  });

  test('listByConversation 隔離 conversation scope 並 trim 查詢', () async {
    await repo.append(_analyzeEvent('c1-a', conversationId: 'c-1'));
    await repo.append(_analyzeEvent('c1-b', conversationId: ' c-1 '));
    await repo.append(_analyzeEvent('c2', conversationId: 'c-2'));

    expect(
      repo.listByConversation(' c-1 ').map((e) => e.id).toSet(),
      {'c1-a', 'c1-b'},
    );
    expect(repo.listByConversation(' '), isEmpty);
  });

  test('append 超過 500 筆 → 刪最舊、留最新 500', () async {
    for (var i = 0; i < 502; i++) {
      await repo.append(_analyzeEvent(
        'e-$i',
        createdAt: DateTime.utc(2026, 1, 1).add(Duration(minutes: i)),
      ));
    }

    final all = repo.listRecent();
    expect(all.length, 500);
    expect(all.first.id, 'e-501'); // 最新保留
    expect(all.any((e) => e.id == 'e-0'), isFalse); // 最舊被剪
    expect(all.any((e) => e.id == 'e-1'), isFalse);
    expect(all.any((e) => e.id == 'e-2'), isTrue);
  });

  test('clearAll 清空', () async {
    await repo.append(_analyzeEvent('e-1'));
    await repo.clearAll();
    expect(repo.listRecent(), isEmpty);
  });
}
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/unit/features/analysis_history/data/repositories/analysis_history_repository_impl_test.dart
```

預期：編譯失敗（repository 檔不存在）。

### Step 3: 最小實作

```dart
// lib/features/analysis_history/domain/repositories/analysis_history_repository.dart
import '../entities/analysis_history_event.dart';

abstract class AnalysisHistoryRepository {
  /// newest-first（模板慣例）；報告頁要升序自己 reverse/sort。
  List<AnalysisHistoryEvent> listRecent({int? limit});

  List<AnalysisHistoryEvent> listByKind(AnalysisHistoryKind kind, {int? limit});

  List<AnalysisHistoryEvent> listByConversation(
    String conversationId, {
    int? limit,
  });

  /// append-only；寫入後超過上限（500）刪最舊。
  Future<void> append(AnalysisHistoryEvent event);

  Future<void> clearAll();
}
```

```dart
// lib/features/analysis_history/data/repositories/analysis_history_repository_impl.dart
import 'package:hive_ce/hive_ce.dart';

import '../../domain/entities/analysis_history_event.dart';
import '../../domain/repositories/analysis_history_repository.dart';

class AnalysisHistoryRepositoryImpl implements AnalysisHistoryRepository {
  AnalysisHistoryRepositoryImpl(this._box);

  /// 保留策略（設計拍板）：超過 500 筆刪最舊。
  static const maxEvents = 500;

  final Box<AnalysisHistoryEvent> _box;

  @override
  List<AnalysisHistoryEvent> listRecent({int? limit}) {
    return _sorted(_box.values, limit: limit);
  }

  @override
  List<AnalysisHistoryEvent> listByKind(
    AnalysisHistoryKind kind, {
    int? limit,
  }) {
    return _sorted(
      _box.values.where((event) => event.kind == kind),
      limit: limit,
    );
  }

  @override
  List<AnalysisHistoryEvent> listByConversation(
    String conversationId, {
    int? limit,
  }) {
    final normalized = AnalysisHistoryEvent.normalizeScope(conversationId);
    if (normalized == null) return const [];
    return _sorted(
      _box.values.where(
        (event) =>
            AnalysisHistoryEvent.normalizeScope(event.conversationId) ==
            normalized,
      ),
      limit: limit,
    );
  }

  @override
  Future<void> append(AnalysisHistoryEvent event) async {
    await _box.put(event.id, event);
    await _pruneIfNeeded();
  }

  @override
  Future<void> clearAll() async {
    await _box.clear();
  }

  Future<void> _pruneIfNeeded() async {
    final overflow = _box.length - maxEvents;
    if (overflow <= 0) return;
    final entries = <MapEntry<dynamic, DateTime>>[];
    for (final key in _box.keys) {
      final event = _box.get(key);
      if (event != null) entries.add(MapEntry(key, event.createdAt));
    }
    entries.sort((a, b) => a.value.compareTo(b.value)); // 最舊在前
    await _box.deleteAll(
      entries.take(overflow).map((entry) => entry.key).toList(growable: false),
    );
  }

  static List<AnalysisHistoryEvent> _sorted(
    Iterable<AnalysisHistoryEvent> events, {
    int? limit,
  }) {
    final sorted = events.toList(growable: false)
      ..sort((a, b) => b.createdAt.compareTo(a.createdAt));
    if (limit == null || limit >= sorted.length) return sorted;
    if (limit <= 0) return const [];
    return sorted.take(limit).toList(growable: false);
  }
}
```

### Step 4: 跑測試確認過

```bash
flutter test test/unit/features/analysis_history/data/repositories/analysis_history_repository_impl_test.dart
```

預期：5 tests passed。

### Step 5: Commit

```bash
git add lib/features/analysis_history/domain/repositories/analysis_history_repository.dart \
        lib/features/analysis_history/data/repositories/analysis_history_repository_impl.dart \
        test/unit/features/analysis_history/data/repositories/analysis_history_repository_impl_test.dart
git commit -m "案2批1 歷史事件 repository：append＋500 筆剪枝＋kind/conversation 查詢"
git push
```

---

## Task 3：Provider＋Hive 註冊三點（storage_service＋app_constants；registrar 已在 Task 1 由 build_runner 更新）

**為什麼**：box 要在 app 啟動時以 AES 加密開啟（照抄 `storage_service.dart:96-99` 的 coachingOutcomeEventsBox 開法），adapter 要手動註冊（storage_service 不用 registrar extension）。這是高風險區（Hive 註冊），完成後整案要 Codex 雙審。

**Files:**
- Create: `lib/features/analysis_history/data/providers/analysis_history_providers.dart`
- Modify: `lib/core/constants/app_constants.dart`（41-46 行 Local Storage 區塊加一行）
- Modify: `lib/core/services/storage_service.dart`（import 區＋55 行後 adapter 註冊＋99 行後 openBox＋box getter＋`clearAll()`）
- Test: `test/unit/services/storage_service_clear_all_test.dart`（既有檔案，補 analysis_history 覆蓋）

### Step 1: 寫失敗測試

改既有 `test/unit/services/storage_service_clear_all_test.dart`——該檔模式是：`setUpAll` 逐 typeId 註冊 adapter → 開出與 `StorageService` getter 同名的 box → seed 資料 → 呼叫 `StorageService.clearAll()` → 斷言各 box 空。比照檔內 `coaching_outcome_events`（typeId 18-21）的三處各加 analysis_history 對應行：

1. `setUpAll` 加：

```dart
    if (!Hive.isAdapterRegistered(24)) {
      Hive.registerAdapter(AnalysisHistoryEventAdapter());
    }
    if (!Hive.isAdapterRegistered(25)) {
      Hive.registerAdapter(AnalysisHistoryKindAdapter());
    }
```

（import 加 `import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';`）

2. box 開啟處（找檔內 `openBox<CoachingOutcomeEvent>` 的那行，旁邊加）：

```dart
    await Hive.openBox<AnalysisHistoryEvent>(
      AppConstants.analysisHistoryEventsBox,
    );
```

3. seed＋斷言（找檔內 seed coaching outcome event 的段落，比照加）：

```dart
    // seed（放進既有 seed 區塊）
    await StorageService.analysisHistoryEventsBox.put(
      'h-1',
      AnalysisHistoryEvent.analyze(
        id: 'h-1',
        createdAt: DateTime.utc(2026, 7, 6),
        conversationId: 'c-1',
        enthusiasmScore: 60,
      ),
    );

    // 斷言（放進既有 clearAll 後斷言區塊）
    expect(StorageService.analysisHistoryEventsBox.isEmpty, isTrue);
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/unit/services/storage_service_clear_all_test.dart
```

預期：編譯失敗（`AppConstants.analysisHistoryEventsBox`、`StorageService.analysisHistoryEventsBox` 不存在）。

### Step 3: 最小實作

`lib/core/constants/app_constants.dart`——`coachingOutcomeEventsBox` 那行（45 行）後面加：

```dart
  static const analysisHistoryEventsBox = 'analysis_history_events';
```

`lib/core/services/storage_service.dart` 四處：

(a) import 區（第 7 行 coaching_outcome import 後）加：

```dart
import '../../features/analysis_history/domain/entities/analysis_history_event.dart';
```

(b) adapter 註冊（55 行 `PracticeSessionAdapter` 後）加：

```dart
    Hive.registerAdapter(AnalysisHistoryEventAdapter()); // typeId=24, 案2 歷史表
    Hive.registerAdapter(AnalysisHistoryKindAdapter()); // typeId=25, 案2 歷史表
```

(c) openBox（96-99 行 CoachingOutcomeEvent 開箱後）加，AES 開法照抄：

```dart
    await Hive.openBox<AnalysisHistoryEvent>(
      AppConstants.analysisHistoryEventsBox,
      encryptionCipher: HiveAesCipher(encryptionKey),
    );
```

(d) getter（`coachingOutcomeEventsBox` getter 旁）＋ `clearAll()`（`coachingOutcomeEventsBox.clear()` 那行後）加：

```dart
  static Box<AnalysisHistoryEvent> get analysisHistoryEventsBox =>
      Hive.box<AnalysisHistoryEvent>(AppConstants.analysisHistoryEventsBox);
```

```dart
    await analysisHistoryEventsBox.clear();
```

（`clearAll()` 的 doc comment 同步補上 analysis history events。）

Provider（照抄 `coaching_outcome_providers.dart:14-17`）：

```dart
// lib/features/analysis_history/data/providers/analysis_history_providers.dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../../core/services/storage_service.dart';
import '../../domain/repositories/analysis_history_repository.dart';
import '../repositories/analysis_history_repository_impl.dart';

final analysisHistoryRepositoryProvider =
    Provider<AnalysisHistoryRepository>((ref) {
  return AnalysisHistoryRepositoryImpl(
    StorageService.analysisHistoryEventsBox,
  );
});
```

### Step 4: 跑測試確認過

```bash
flutter test test/unit/services/storage_service_clear_all_test.dart
flutter test test/unit/features/analysis_history/
```

預期：全綠。

### Step 5: Commit

```bash
git add lib/features/analysis_history/data/providers/analysis_history_providers.dart \
        lib/core/constants/app_constants.dart \
        lib/core/services/storage_service.dart \
        test/unit/services/storage_service_clear_all_test.dart
git commit -m "案2批1 provider＋Hive 註冊：analysis_history_events AES box＋clearAll 納管"
git push
```

---

## Task 4：analyze hook（掛 `_persistLatestAnalysisSnapshot` 末尾，best-effort）

**為什麼**：`_persistLatestAnalysisSnapshot` 的兩個呼叫端（listener 路徑 3804 行、hydrate 路徑 903 行）都被 891-896 行的 `alreadyPersisted` gate / listener 一次性保護——掛在方法內自然繼承「同一次分析絕不重複記錄」，不另做冪等。失敗只 debugPrint 絕不 rethrow，分析呈現完全不受影響。

**Files:**
- Create: `test/helpers/memory_analysis_history_repository.dart`（in-memory fake，模板＝`test/helpers/memory_coaching_outcome_repository.dart`）
- Modify: `lib/features/analysis/presentation/screens/analysis_screen.dart`（`_persistLatestAnalysisSnapshot` 1290-1319 行末尾＋import 兩行）
- Test: `test/widget/features/analysis/analysis_screen_hydration_test.dart`（既有檔案，harness 加 override＋兩個新 testWidgets）

### Step 1: 寫失敗測試

先建 fake：

```dart
// test/helpers/memory_analysis_history_repository.dart
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';
import 'package:vibesync/features/analysis_history/domain/repositories/analysis_history_repository.dart';

class MemoryAnalysisHistoryRepository implements AnalysisHistoryRepository {
  final events = <AnalysisHistoryEvent>[];

  @override
  Future<void> append(AnalysisHistoryEvent event) async {
    events.add(event);
  }

  @override
  Future<void> clearAll() async => events.clear();

  @override
  List<AnalysisHistoryEvent> listRecent({int? limit}) => _limit(
        events.toList()..sort((a, b) => b.createdAt.compareTo(a.createdAt)),
        limit,
      );

  @override
  List<AnalysisHistoryEvent> listByKind(AnalysisHistoryKind kind,
          {int? limit}) =>
      _limit(listRecent().where((e) => e.kind == kind).toList(), limit);

  @override
  List<AnalysisHistoryEvent> listByConversation(String conversationId,
      {int? limit}) {
    final normalized = AnalysisHistoryEvent.normalizeScope(conversationId);
    if (normalized == null) return const [];
    return _limit(
      listRecent()
          .where((e) =>
              AnalysisHistoryEvent.normalizeScope(e.conversationId) ==
              normalized)
          .toList(),
      limit,
    );
  }

  static List<AnalysisHistoryEvent> _limit(
      List<AnalysisHistoryEvent> list, int? limit) {
    if (limit == null || limit >= list.length) return list;
    if (limit <= 0) return const [];
    return list.take(limit).toList();
  }
}
```

改 `analysis_screen_hydration_test.dart`（該檔已有完整 harness，見 481-516 行 `_pumpHydratedAnalysisScreenWithRepo`）：

1. import 加：

```dart
import 'package:vibesync/features/analysis_history/data/providers/analysis_history_providers.dart';
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';
import '../../../helpers/memory_analysis_history_repository.dart';
```

2. 找到 `_HydrationHarness` class（檔內 grep `class _HydrationHarness`），加欄位 `final MemoryAnalysisHistoryRepository history;` 並補建構子參數。

3. `_pumpHydratedAnalysisScreenWithRepo` 內（494 行 overrides 列表）：函式開頭建 `final history = MemoryAnalysisHistoryRepository();`，overrides 加一行：

```dart
        analysisHistoryRepositoryProvider.overrideWithValue(history),
```

回傳處把 `history` 塞進 harness。

4. 檔尾新增 group（比照 905-1010 行「done hydrate persists」兩個既有測試的 seed 寫法，`_fullRawResponse()` 的 enthusiasm score=72、gameStage current='premise'、`_conversation()` name='小雲'）：

```dart
  group('案2：analyze 歷史事件 hook', () {
    testWidgets('hydrate persist 成功 → 寫入一筆 analyze 歷史事件', (tester) async {
      final raw = _fullRawResponse();
      final conv = _conversation(); // 無既有 snapshot → 會走 persist

      final harness = await _pumpHydratedAnalysisScreenWithRepo(
        tester,
        seed: StreamingAnalysisState(
          phase: StreamingAnalyzePhase.done,
          recommendationPreview: _preview(runId: 'run_history_write'),
          full: _fullWithRawResponse(raw),
          analysisRunId: 'run_history_write',
        ),
        conversation: conv,
      );

      tester.takeException();
      await tester.pump(const Duration(milliseconds: 1));

      expect(harness.history.events.length, 1);
      final event = harness.history.events.single;
      expect(event.kind, AnalysisHistoryKind.analyze);
      expect(event.conversationId, _conversationId);
      expect(event.subjectName, '小雲');
      expect(event.enthusiasmScore, 72);
      expect(event.gameStageLabel, 'premise');
    });

    testWidgets('alreadyPersisted gate 命中 → 不寫歷史事件（去重繼承）', (tester) async {
      final raw = _fullRawResponse();
      final conv = _conversation(
        lastAnalysisSnapshotJson: jsonEncode(raw),
        lastAnalyzedMessageCount: 1,
        lastEnthusiasmScore: 72,
      );

      final harness = await _pumpHydratedAnalysisScreenWithRepo(
        tester,
        seed: StreamingAnalysisState(
          phase: StreamingAnalyzePhase.done,
          recommendationPreview: _preview(runId: 'run_history_dedupe'),
          full: _fullWithRawResponse(raw),
          analysisRunId: 'run_history_dedupe',
        ),
        conversation: conv,
      );

      tester.takeException();
      await tester.pump(const Duration(milliseconds: 1));

      expect(harness.history.events, isEmpty);
    });
  });
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/widget/features/analysis/analysis_screen_hydration_test.dart
```

預期：第一個新測試失敗（`events.length` 是 0 不是 1）；第二個可能已過（本來就沒寫）。既有測試必須全部維持綠——若紅了代表 harness 改壞，先修 harness。

### Step 3: 最小實作

`analysis_screen.dart` import 區加：

```dart
import '../../../analysis_history/data/providers/analysis_history_providers.dart';
import '../../../analysis_history/domain/entities/analysis_history_event.dart';
```

（相對路徑深度以檔內既有 feature 間 import 為準，該檔在 `lib/features/analysis/presentation/screens/`，故為 `../../../analysis_history/...`。）

`_persistLatestAnalysisSnapshot`（1290-1319 行）末尾、`await ref.read(conversationWriteControllerProvider.notifier).save(conv);` 之後加：

```dart
    // 案2：analyze 歷史事件（best-effort：失敗只 debugPrint，絕不 rethrow，
    // 分析呈現完全不受影響）。去重靠呼叫端既有 gate（hydrate 路徑
    // _maybePersistAndSyncOnHydrate 的 alreadyPersisted 比對＋listener 路徑
    // 一次性觸發），同一次分析絕不重複記錄，這裡不另做冪等。
    try {
      await ref.read(analysisHistoryRepositoryProvider).append(
            AnalysisHistoryEvent.analyze(
              id: const Uuid().v4(),
              createdAt: DateTime.now(),
              conversationId: widget.conversationId,
              subjectName: conv.name,
              enthusiasmScore: result.enthusiasmScore,
              gameStageLabel: result.gameStage.current.name,
            ),
          );
    } catch (e) {
      debugPrint('AnalysisHistory analyze append failed: $e');
    }
```

（`Uuid` 該檔已 import；`debugPrint` 隨 material import 可用。方法本身在兩個呼叫端都是 fire-and-forget（`.catchError((_) {})`／`unawaited`），try-catch 已保證 hook 失敗不會冒泡，不掛 loading state。）

### Step 4: 跑測試確認過

```bash
flutter test test/widget/features/analysis/analysis_screen_hydration_test.dart
```

預期：全綠（含既有 hydration 測試）。

### Step 5: Commit

```bash
git add lib/features/analysis/presentation/screens/analysis_screen.dart \
        test/helpers/memory_analysis_history_repository.dart \
        test/widget/features/analysis/analysis_screen_hydration_test.dart
git commit -m "案2批1 analyze hook：快照持久化末尾 best-effort 落歷史事件（繼承既有去重 gate）"
git push
```

---

## Task 5：practice hook（`_persist()` 內、session 存檔成功之後；temperatureScore==null 不寫）

**為什麼**：練習溫度只在新手模式有值；非新手模式三元組全 null，是畫不出來的空點，設計拍板**只在 `temperatureScore != null` 時寫事件**。`PracticeChatController` 沒有 Ref，repository 走建構子注入（比照 `onProfileUnlocked` 等既有選配依賴），測試可直接塞 memory fake。

**Files:**
- Modify: `lib/features/practice_chat/data/providers/practice_chat_providers.dart`
  - `PracticeChatController` 建構子（288-346 行）加選配參數
  - `_persist()`（1249-1277 行）末尾加 hook
  - `practiceChatControllerProvider`（1380-1400 行）wire 真 repository
  - import 區加 uuid／foundation／analysis_history（缺哪個補哪個）
- Test: `test/unit/features/practice_chat/data/providers/practice_chat_controller_test.dart`（既有檔案）

### Step 1: 寫失敗測試

在 `practice_chat_controller_test.dart`：

1. import 加：

```dart
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';
import '../../../../../helpers/memory_analysis_history_repository.dart';
```

2. `main()` 的 `late` 區加 `late MemoryAnalysisHistoryRepository history;`，`setUp` 內加 `history = MemoryAnalysisHistoryRepository();`。

3. `makeController`（約 151-169 行）的 `PracticeChatController(...)` 呼叫加一行 `historyRepository: history,`（`makeControllerFrom` 同樣加）。

4. 新 group（**先讀檔內既有 group `'beginner learning mode'`（約 1241 行起）與 group `'送訊息'`（約 713 行起）**，照抄它們「翻牌→送訊息」的驅動方式與 `_reply(...)` helper——helper 約在 273-291 行，有 `temperatureScore` 參數）：

```dart
  group('案2：practice 溫度歷史事件', () {
    test('beginner 模式收到溫度 → _persist 落一筆 practice 事件', () async {
      final c = makeController();
      await c.drawNewPracticeGirl(); // 比照既有測試的翻牌驅動；名稱以檔內為準
      await c.setPracticeLearningMode(PracticeLearningMode.beginner);
      api.sendHandler = (turns, {profile}) async => _reply(
            temperatureScore: 38,
            familiarityScore: 12,
            relationshipStageLabel: '破冰',
          ); // _reply 參數名以檔內 helper 為準
      await c.sendMessage('嗨');

      expect(history.events, isNotEmpty);
      final event = history.events.last;
      expect(event.kind, AnalysisHistoryKind.practice);
      expect(event.profileId, isNotNull);
      expect(event.roundIndex, 1);
      expect(event.temperatureScore, 38);
    });

    test('standard 模式（temperatureScore null）→ 絕不寫事件', () async {
      final c = makeController();
      await c.drawNewPracticeGirl();
      api.sendHandler = (turns, {profile}) async => _reply();
      await c.sendMessage('嗨');

      expect(history.events, isEmpty);
    });

    test('endPractice（debrief 後 _persist）也只在有溫度時寫', () async {
      final c = makeController();
      await c.drawNewPracticeGirl();
      api.sendHandler = (turns, {profile}) async => _reply();
      api.debriefHandler = (turns, {profile}) async => _debrief(); // 檔內既有 helper
      await c.sendMessage('嗨');
      await c.endPractice();

      expect(history.events, isEmpty); // standard 全程無溫度
    });
  });
```

（驅動細節——翻牌方法名、`_reply`/`_debrief` helper 簽名——**一律以檔內既有測試為準**，上面是意圖骨架；斷言部分照抄。）

### Step 2: 跑測試確認失敗

```bash
flutter test test/unit/features/practice_chat/data/providers/practice_chat_controller_test.dart
```

預期：編譯失敗（`historyRepository` 參數不存在）。

### Step 3: 最小實作

`practice_chat_providers.dart`：

(a) import 加（檔頂）：

```dart
import 'package:flutter/foundation.dart';
import 'package:uuid/uuid.dart';

import '../../../analysis_history/data/providers/analysis_history_providers.dart';
import '../../../analysis_history/domain/entities/analysis_history_event.dart';
import '../../../analysis_history/domain/repositories/analysis_history_repository.dart';
```

（若 `foundation`/`uuid` 已有就不重複；`debugPrint` 在 foundation。）

(b) `PracticeChatController` 公開建構子（288-315 行）參數列加 `AnalysisHistoryRepository? historyRepository,`，轉傳給 `this._`；私有建構子（317-346 行）加 `required AnalysisHistoryRepository? historyRepository,` 與 `_historyRepository = historyRepository,`；欄位區（348-356 行）加：

```dart
  final AnalysisHistoryRepository? _historyRepository;
```

(c) `_persist()`（1249-1277 行）改為存檔成功後呼叫 hook：

```dart
  Future<void> _persist() async {
    final s = state;
    final girl = s.girl;
    if (girl == null) return; // 防呆：未翻牌不持久化
    await _repo.save(PracticeSession(
      // …既有欄位完全不動…
    ));
    await _recordPracticeHistoryEvent(s, girl.profileId);
  }

  /// 案2：練習溫度歷史事件（best-effort：失敗只 debugPrint 絕不 rethrow，
  /// 收操流程完全不受影響）。只在新手模式且 temperatureScore 有值時寫——
  /// 非新手模式三元組全 null，是畫不出來的空點（設計拍板）。
  Future<void> _recordPracticeHistoryEvent(
    PracticeChatState s,
    String profileId,
  ) async {
    final history = _historyRepository;
    if (history == null) return;
    final temperature = s.isBeginnerMode ? s.temperatureScore : null;
    if (temperature == null) return;
    try {
      await history.append(AnalysisHistoryEvent.practice(
        id: const Uuid().v4(),
        createdAt: DateTime.now(),
        profileId: profileId,
        roundIndex: s.roundIndex,
        temperatureScore: temperature,
        familiarityScore: s.isBeginnerMode ? s.familiarityScore : null,
        relationshipStageLabel:
            s.isBeginnerMode ? s.relationshipStageLabel : null,
      ));
    } catch (e) {
      debugPrint('AnalysisHistory practice append failed: $e');
    }
  }
```

（溫度三元組取值與 `_persist` 既有 `isBeginnerMode ? x : null` 寫法逐字一致，1271-1274 行。）

(d) `practiceChatControllerProvider`（1383 行 `PracticeChatController(` 呼叫內）加：

```dart
    historyRepository: ref.read(analysisHistoryRepositoryProvider),
```

### Step 4: 跑測試確認過

```bash
flutter test test/unit/features/practice_chat/data/providers/practice_chat_controller_test.dart
```

預期：全綠（含既有 60+ 測試——hook 是純附加，任何既有紅燈都代表改壞了，回頭查）。

### Step 5: Commit

```bash
git add lib/features/practice_chat/data/providers/practice_chat_providers.dart \
        test/unit/features/practice_chat/data/providers/practice_chat_controller_test.dart
git commit -m "案2批1 practice hook：_persist 後 best-effort 落溫度事件（僅新手模式有溫度時）"
git push
```

**批1 完成檢查點**：`flutter test test/unit/features/analysis_history/ test/unit/services/storage_service_clear_all_test.dart test/widget/features/analysis/analysis_screen_hydration_test.dart test/unit/features/practice_chat/` 全綠。

---

## Task 6：report_data_service 新資料流（對象清單＋單對象序列＋practice 全域序列）

**為什麼**：熱度趨勢資料源要從「Conversation 最新分數 × 7 筆」換成事件表（A 案核心）。**既有 `generateReport` 的 averageScore/scoreDelta/comparisons/stageDistributions/totalConversations 一行都不動**（範圍收斂拍板）。空狀態判定（<2 筆不畫圖）放 UI 層，服務層只回列表。

**Files:**
- Modify: `lib/features/report/domain/entities/report_models.dart`（加 `AnalysisSubject`）
- Modify: `lib/features/report/data/services/report_data_service.dart`（加三個純函式）
- Modify: `lib/features/report/data/providers/report_providers.dart`（加四個 provider）
- Test: `test/unit/features/report/report_data_service_history_test.dart`（新檔）

### Step 1: 寫失敗測試

```dart
// test/unit/features/report/report_data_service_history_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:vibesync/features/analysis_history/domain/entities/analysis_history_event.dart';
import 'package:vibesync/features/report/data/services/report_data_service.dart';

AnalysisHistoryEvent _analyze(
  String id,
  String conversationId,
  String name,
  int score,
  DateTime createdAt,
) =>
    AnalysisHistoryEvent.analyze(
      id: id,
      createdAt: createdAt,
      conversationId: conversationId,
      subjectName: name,
      enthusiasmScore: score,
      gameStageLabel: 'premise',
    );

AnalysisHistoryEvent _practice(String id, int? temperature, DateTime createdAt) =>
    AnalysisHistoryEvent.practice(
      id: id,
      createdAt: createdAt,
      profileId: 'practice_girl_001',
      roundIndex: 1,
      temperatureScore: temperature,
    );

void main() {
  final service = ReportDataService();

  group('analysisSubjects', () {
    test('distinct conversationId、按最近事件 desc、名字取最新快照', () {
      final subjects = service.analysisSubjects([
        _analyze('a1', 'c-1', '小雲', 50, DateTime(2026, 6, 1)),
        _analyze('a2', 'c-2', '安安', 60, DateTime(2026, 6, 5)),
        _analyze('a3', 'c-1', '小雲改名', 70, DateTime(2026, 6, 9)),
        _practice('p1', 30, DateTime(2026, 6, 30)), // practice 不入對象清單
      ]);

      expect(subjects.map((s) => s.conversationId), ['c-1', 'c-2']);
      expect(subjects.first.name, '小雲改名'); // 最新快照
    });

    test('空事件 → 空清單', () {
      expect(service.analysisSubjects(const []), isEmpty);
    });
  });

  group('subjectTrendPoints', () {
    test('單對象、createdAt 升序、跳過 null 分數', () {
      final points = service.subjectTrendPoints([
        _analyze('a2', 'c-1', '小雲', 70, DateTime(2026, 6, 9)),
        _analyze('a1', 'c-1', '小雲', 50, DateTime(2026, 6, 1)),
        _analyze('b1', 'c-2', '安安', 99, DateTime(2026, 6, 2)), // 別的對象
        AnalysisHistoryEvent.analyze(
          id: 'a-null',
          createdAt: DateTime(2026, 6, 3),
          conversationId: 'c-1',
          subjectName: '小雲',
          enthusiasmScore: null, // 無分數 → 跳過
        ),
      ], 'c-1');

      expect(points.map((p) => p.score), [50, 70]); // 升序
      expect(points.map((p) => p.date),
          [DateTime(2026, 6, 1), DateTime(2026, 6, 9)]);
    });
  });

  group('practiceTemperaturePoints', () {
    test('全域混排、升序、跳過 null 溫度', () {
      final points = service.practiceTemperaturePoints([
        _practice('p2', 45, DateTime(2026, 6, 9)),
        _practice('p1', 30, DateTime(2026, 6, 1)),
        _practice('p-null', null, DateTime(2026, 6, 5)),
        _analyze('a1', 'c-1', '小雲', 50, DateTime(2026, 6, 2)), // analyze 不入
      ]);

      expect(points.map((p) => p.score), [30, 45]);
    });

    test('全空 → 空清單（UI 依此顯示引導文案）', () {
      expect(service.practiceTemperaturePoints(const []), isEmpty);
    });
  });
}
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/unit/features/report/report_data_service_history_test.dart
```

預期：編譯失敗（`analysisSubjects` 等方法不存在）。

### Step 3: 最小實作

`report_models.dart` 檔尾加：

```dart
/// 案2：報告頁對象選擇器項目（來自 analyze 歷史事件的 distinct conversationId）。
class AnalysisSubject {
  final String conversationId;
  final String name;
  final DateTime lastEventAt;

  const AnalysisSubject({
    required this.conversationId,
    required this.name,
    required this.lastEventAt,
  });
}
```

`report_data_service.dart` import 加 `import '../../../analysis_history/domain/entities/analysis_history_event.dart';`，class 內加（`generateReport` 不動）：

```dart
  /// 案2：對象清單——distinct conversationId（取最新 subjectName 快照），
  /// 按最近事件時間 desc（報告頁 chip 列預設選第一個＝最近分析過的對象）。
  List<AnalysisSubject> analysisSubjects(List<AnalysisHistoryEvent> events) {
    final latestByConversation = <String, AnalysisHistoryEvent>{};
    for (final event in events) {
      if (event.kind != AnalysisHistoryKind.analyze) continue;
      final id = AnalysisHistoryEvent.normalizeScope(event.conversationId);
      if (id == null) continue;
      final existing = latestByConversation[id];
      if (existing == null || event.createdAt.isAfter(existing.createdAt)) {
        latestByConversation[id] = event;
      }
    }
    return latestByConversation.entries
        .map((entry) => AnalysisSubject(
              conversationId: entry.key,
              name: entry.value.subjectName ?? '未命名對象',
              lastEventAt: entry.value.createdAt,
            ))
        .toList()
      ..sort((a, b) => b.lastEventAt.compareTo(a.lastEventAt));
  }

  /// 案2：單對象熱度時間序列（createdAt 升序；enthusiasmScore null 跳過）。
  List<HeatTrendPoint> subjectTrendPoints(
    List<AnalysisHistoryEvent> events,
    String conversationId,
  ) {
    final id = AnalysisHistoryEvent.normalizeScope(conversationId);
    if (id == null) return const [];
    return events
        .where((event) =>
            event.kind == AnalysisHistoryKind.analyze &&
            AnalysisHistoryEvent.normalizeScope(event.conversationId) == id &&
            event.enthusiasmScore != null)
        .map((event) => HeatTrendPoint(
              date: event.createdAt,
              score: event.enthusiasmScore!,
              conversationName: event.subjectName ?? '',
            ))
        .toList()
      ..sort((a, b) => a.date.compareTo(b.date));
  }

  /// 案2：練習溫度全域時間序列——刻意不分對象混排（練習溫度量的是玩家
  /// 本人的開場→升溫能力，跨對象看斜率才是成長曲線）。temperatureScore
  /// null（非新手模式）跳過。familiarityScore 不畫第二條線（YAGNI）。
  List<HeatTrendPoint> practiceTemperaturePoints(
    List<AnalysisHistoryEvent> events,
  ) {
    return events
        .where((event) =>
            event.kind == AnalysisHistoryKind.practice &&
            event.temperatureScore != null)
        .map((event) => HeatTrendPoint(
              date: event.createdAt,
              score: event.temperatureScore!,
              conversationName: '',
            ))
        .toList()
      ..sort((a, b) => a.date.compareTo(b.date));
  }
```

`report_providers.dart` 加（import `analysis_history` 的 entity＋providers）：

```dart
/// 案2：歷史事件快照。autoDispose——離開報告頁釋放，重進重讀 box（Hive
/// box 非 reactive，靠頁面生命週期刷新）。
final analysisHistoryEventsProvider =
    Provider.autoDispose<List<AnalysisHistoryEvent>>((ref) {
  return ref.watch(analysisHistoryRepositoryProvider).listRecent();
});

final analysisSubjectsProvider =
    Provider.autoDispose<List<AnalysisSubject>>((ref) {
  return ref.watch(reportDataServiceProvider).analysisSubjects(
        ref.watch(analysisHistoryEventsProvider),
      );
});

final subjectHeatTrendProvider = Provider.autoDispose
    .family<List<HeatTrendPoint>, String>((ref, conversationId) {
  return ref.watch(reportDataServiceProvider).subjectTrendPoints(
        ref.watch(analysisHistoryEventsProvider),
        conversationId,
      );
});

final practiceTemperatureTrendProvider =
    Provider.autoDispose<List<HeatTrendPoint>>((ref) {
  return ref.watch(reportDataServiceProvider).practiceTemperaturePoints(
        ref.watch(analysisHistoryEventsProvider),
      );
});

/// 案2：對象選擇器選中的 conversationId；null＝預設（清單第一個）。
final selectedReportSubjectProvider =
    StateProvider.autoDispose<String?>((ref) => null);
```

### Step 4: 跑測試確認過

```bash
flutter test test/unit/features/report/report_data_service_history_test.dart
flutter test test/widget/features/report/my_report_screen_test.dart
```

預期：新檔全綠；既有報告頁測試不受影響（本 task 未動畫面）。

### Step 5: Commit

```bash
git add lib/features/report/domain/entities/report_models.dart \
        lib/features/report/data/services/report_data_service.dart \
        lib/features/report/data/providers/report_providers.dart \
        test/unit/features/report/report_data_service_history_test.dart
git commit -m "案2批2 report 資料流：對象清單＋單對象熱度序列＋練習溫度全域序列"
git push
```

---

## Task 7：heat_trend_chart x 軸序號→真日期

**為什麼**：現行 FlSpot x 是 0..n-1 序號（`heat_trend_chart.dart:162-165`），點距不反映真實間隔。改成「距首點天數」，底部標籤沿用 `DateFormat('M/dd')`。

**Files:**
- Modify: `lib/features/report/presentation/widgets/heat_trend_chart.dart`（`_buildChart` 158-182 行、`_titlesData` 224-276 行；`_touchData` 的 spotIndex 對應 sorted index，不用改）
- Test: `test/widget/features/report/heat_trend_chart_test.dart`（新檔）

### Step 1: 寫失敗測試

```dart
// test/widget/features/report/heat_trend_chart_test.dart
import 'package:fl_chart/fl_chart.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:vibesync/features/report/domain/entities/report_models.dart';
import 'package:vibesync/features/report/presentation/widgets/heat_trend_chart.dart';

Future<void> _pump(WidgetTester tester, List<HeatTrendPoint> points) async {
  await tester.pumpWidget(MaterialApp(
    home: Scaffold(
      body: SingleChildScrollView(
        child: HeatTrendChart(
          trendPoints: points,
          averageScore: 60,
          scoreDelta: 5,
        ),
      ),
    ),
  ));
  // 鐵則：動畫必收斂（LineChart 首繪無無限動畫，pumpAndSettle 必須過）。
  await tester.pumpAndSettle();
}

void main() {
  testWidgets('x 軸用距首點天數：6/1、6/2、6/5 → x = 0, 1, 4', (tester) async {
    await _pump(tester, [
      HeatTrendPoint(date: DateTime(2026, 6, 2), score: 60, conversationName: 'A'),
      HeatTrendPoint(date: DateTime(2026, 6, 1), score: 50, conversationName: 'A'),
      HeatTrendPoint(date: DateTime(2026, 6, 5), score: 80, conversationName: 'A'),
    ]);

    final chart = tester.widget<LineChart>(find.byType(LineChart));
    final spots = chart.data.lineBarsData.single.spots;
    expect(spots.map((s) => s.x), [0.0, 1.0, 4.0]); // 點距反映真實間隔
    expect(spots.map((s) => s.y), [50.0, 60.0, 80.0]); // 仍按日期升序
  });

  testWidgets('底部標籤是真日期 M/dd', (tester) async {
    await _pump(tester, [
      HeatTrendPoint(date: DateTime(2026, 6, 1), score: 50, conversationName: 'A'),
      HeatTrendPoint(date: DateTime(2026, 6, 5), score: 80, conversationName: 'A'),
    ]);

    expect(find.text('6/01'), findsOneWidget);
    expect(find.text('6/05'), findsOneWidget);
  });

  testWidgets('空清單 → 空狀態不畫圖', (tester) async {
    await _pump(tester, const []);
    expect(find.byType(LineChart), findsNothing);
    expect(find.text('尚無數據'), findsOneWidget);
  });
}
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/widget/features/report/heat_trend_chart_test.dart
```

預期：第一個測試失敗（現行 x 是 0,1,2 不是 0,1,4）；第二個失敗（現行標籤按序號 interval，6/05 未必渲染）。

### Step 3: 最小實作

`_buildChart()` 換成（158-182 行整段替換）：

```dart
  Widget _buildChart() {
    final sorted = List<HeatTrendPoint>.from(trendPoints)
      ..sort((a, b) => a.date.compareTo(b.date));

    final firstDate = sorted.first.date;
    // x＝距首點天數（分鐘精度轉天，同日多點不撞 x）。
    double xOf(DateTime date) =>
        date.difference(firstDate).inMinutes / (24 * 60.0);

    final spots = [
      for (final point in sorted) FlSpot(xOf(point.date), point.score.toDouble()),
    ];
    // 全部同一時刻（maxX=0）時 fl_chart 需要 minX<maxX，退 1 天刻度。
    final maxX = spots.last.x <= 0 ? 1.0 : spots.last.x;

    return SizedBox(
      height: 180,
      child: LineChart(
        LineChartData(
          minX: 0,
          maxX: maxX,
          minY: 0,
          maxY: 100,
          clipData: const FlClipData.all(),
          gridData: _gridData(),
          titlesData: _titlesData(firstDate, maxX),
          borderData: FlBorderData(show: false),
          lineBarsData: [_lineBarData(spots)],
          lineTouchData: _touchData(sorted),
        ),
      ),
    );
  }
```

`_titlesData` 簽名與 bottomTitles 換成（leftTitles 整段不動）：

```dart
  FlTitlesData _titlesData(DateTime firstDate, double maxX) {
    final dateFormat = DateFormat('M/dd');
    // 最多 ~5 個日期標籤，避免重疊。
    final bottomInterval = maxX <= 4 ? 1.0 : (maxX / 4).ceilToDouble();

    return FlTitlesData(
      topTitles: const AxisTitles(sideTitles: SideTitles(showTitles: false)),
      rightTitles: const AxisTitles(sideTitles: SideTitles(showTitles: false)),
      leftTitles: AxisTitles(
        // …原 230-247 行內容原封不動搬過來…
      ),
      bottomTitles: AxisTitles(
        sideTitles: SideTitles(
          showTitles: true,
          reservedSize: 28,
          interval: bottomInterval,
          getTitlesWidget: (value, meta) {
            if (value < 0 || value > maxX) return const SizedBox.shrink();
            final date =
                firstDate.add(Duration(minutes: (value * 24 * 60).round()));
            return Padding(
              padding: const EdgeInsets.only(top: 6),
              child: Text(
                dateFormat.format(date),
                style: TextStyle(
                  fontSize: 10,
                  color:
                      AppColors.onBackgroundSecondary.withValues(alpha: 0.70),
                ),
              ),
            );
          },
        ),
      ),
    );
  }
```

（`_touchData(sorted)` 用 `spot.spotIndex` 索引 sorted，跟 x 值無關，維持原樣。）

### Step 4: 跑測試確認過

```bash
flutter test test/widget/features/report/heat_trend_chart_test.dart
flutter test test/widget/features/report/my_report_screen_test.dart
```

預期：全綠（既有報告頁測試用 2 個點的 `_paidReport()`，x 軸改動不影響其斷言）。

### Step 5: Commit

```bash
git add lib/features/report/presentation/widgets/heat_trend_chart.dart \
        test/widget/features/report/heat_trend_chart_test.dart
git commit -m "案2批2 heat_trend_chart x 軸序號改真日期（FlSpot x＝距首點天數）"
git push
```

---

## Task 8：對象選擇器 chip 列＋熱度卡接上事件資料源

**為什麼**：A 案核心 UI——橫向 chip 列選對象，預設最近分析過的；熱度卡 trendPoints 換成所選對象的事件序列。**averageScore/scoreDelta 維持現行 Conversation 邏輯**（`reportDataProvider`），只有 trendPoints 換源。所選對象事件 <2 筆 → 卡片顯示引導文案不畫圖。

**Files:**
- Create: `lib/features/report/presentation/widgets/report_subject_selector.dart`
- Modify: `lib/features/report/presentation/widgets/heat_trend_chart.dart`（加選配 `emptyMessage` 參數，兩行）
- Modify: `lib/features/report/presentation/screens/my_report_screen.dart`（76-80 行 HeatTrendChart 區塊前後）
- Test: `test/widget/features/report/my_report_screen_test.dart`（既有檔案加 group）

### Step 1: 寫失敗測試

`my_report_screen_test.dart` import 加 analysis_history entity、report_providers 既有 import 已在。加 helper 與 group：

```dart
AnalysisHistoryEvent _historyEvent(
  String id,
  String conversationId,
  String name,
  int score,
  DateTime createdAt,
) =>
    AnalysisHistoryEvent.analyze(
      id: id,
      createdAt: createdAt,
      conversationId: conversationId,
      subjectName: name,
      enthusiasmScore: score,
      gameStageLabel: 'premise',
    );
```

`_pumpReportScreen` 加選配參數 `List<AnalysisHistoryEvent> historyEvents = const []`，overrides 加：

```dart
        analysisHistoryEventsProvider.overrideWithValue(historyEvents),
```

新 group：

```dart
  group('案2：對象選擇器＋單對象熱度序列', () {
    final events = [
      _historyEvent('a1', 'c-1', '小雲', 50, DateTime(2026, 6, 1)),
      _historyEvent('a2', 'c-1', '小雲', 70, DateTime(2026, 6, 8)),
      _historyEvent('b1', 'c-2', '安安', 40, DateTime(2026, 6, 9)),
      _historyEvent('b2', 'c-2', '安安', 66, DateTime(2026, 6, 10)),
    ];

    testWidgets('chip 列 render、預設選最近分析過的對象（安安）', (tester) async {
      await _pumpReportScreen(
        tester,
        subscription: const SubscriptionState(
          tier: SubscriptionTierHelper.starter,
        ),
        report: _paidReport(),
        partners: [_partner('p1', 'Vivi')],
        historyEvents: events,
      );

      expect(find.text('安安'), findsWidgets);
      expect(find.text('小雲'), findsWidgets);
      // 預設選最近事件的對象 c-2（安安）→ 圖上是安安的 2 點序列
      final chart =
          tester.widget<HeatTrendChart>(find.byType(HeatTrendChart));
      expect(chart.trendPoints.map((p) => p.score), [40, 66]);
    });

    testWidgets('點另一個 chip → 熱度卡切到那位對象的序列', (tester) async {
      await _pumpReportScreen(
        tester,
        subscription: const SubscriptionState(
          tier: SubscriptionTierHelper.starter,
        ),
        report: _paidReport(),
        partners: [_partner('p1', 'Vivi')],
        historyEvents: events,
      );

      await tester.tap(find.text('小雲').first);
      await tester.pumpAndSettle(); // LineChart 資料切換動畫有限時長，必收斂

      final chart =
          tester.widget<HeatTrendChart>(find.byType(HeatTrendChart));
      expect(chart.trendPoints.map((p) => p.score), [50, 70]);
    });

    testWidgets('所選對象事件 <2 筆 → 引導文案、不畫圖', (tester) async {
      await _pumpReportScreen(
        tester,
        subscription: const SubscriptionState(
          tier: SubscriptionTierHelper.starter,
        ),
        report: _paidReport(),
        partners: [_partner('p1', 'Vivi')],
        historyEvents: [
          _historyEvent('a1', 'c-1', '小雲', 50, DateTime(2026, 6, 1)),
        ],
      );

      expect(find.text('再多分析幾次，就能看到這位對象的熱度變化'), findsOneWidget);
    });

    testWidgets('完全沒有事件（舊用戶未回填）→ 引導文案、既有其他區塊照常', (tester) async {
      await _pumpReportScreen(
        tester,
        subscription: const SubscriptionState(
          tier: SubscriptionTierHelper.starter,
        ),
        report: _paidReport(),
        partners: [_partner('p1', 'Vivi')],
        historyEvents: const [],
      );

      expect(find.text('再多分析幾次，就能看到這位對象的熱度變化'), findsOneWidget);
      expect(find.byType(ConversationComparisonChart), findsOneWidget);
      expect(find.byType(StageDistributionChart), findsOneWidget);
    });
  });
```

### Step 2: 跑測試確認失敗

```bash
flutter test test/widget/features/report/my_report_screen_test.dart
```

預期：新測試全紅（chip 列不存在、trendPoints 仍是 `_paidReport()` 的舊資料）。既有兩個決策 A 測試若因 HeatTrendChart 資料源改動而需要調整（它們 historyEvents 預設空 → 現在付費分支顯示引導文案而非 HeatTrendChart），**允許把該斷言改成 `find.byType(HeatTrendChart), findsNothing` ＋引導文案存在**——這是資料源換軌的預期行為，改斷言時在測試註解寫明「案2 資料源換軌」。

### Step 3: 最小實作

(a) `heat_trend_chart.dart` 加選配參數（不破壞既有呼叫）：

```dart
  final String emptyMessage;

  const HeatTrendChart({
    super.key,
    required this.trendPoints,
    required this.averageScore,
    required this.scoreDelta,
    this.emptyMessage = '尚無數據',
  });
```

`_buildEmptyState()` 裡的 `'尚無數據'` 換成 `emptyMessage`。

(b) 新 widget：

```dart
// lib/features/report/presentation/widgets/report_subject_selector.dart
import 'package:flutter/material.dart';

import '../../../../core/theme/app_colors.dart';
import '../../domain/entities/report_models.dart';

/// 案2：報告頁熱度趨勢的對象選擇器（橫向 chip 列）。
class ReportSubjectSelector extends StatelessWidget {
  final List<AnalysisSubject> subjects;
  final String? selectedConversationId;
  final ValueChanged<String> onSelected;

  const ReportSubjectSelector({
    super.key,
    required this.subjects,
    required this.selectedConversationId,
    required this.onSelected,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 36,
      child: ListView.separated(
        scrollDirection: Axis.horizontal,
        itemCount: subjects.length,
        separatorBuilder: (_, __) => const SizedBox(width: 8),
        itemBuilder: (context, index) {
          final subject = subjects[index];
          final selected = subject.conversationId == selectedConversationId;
          return GestureDetector(
            onTap: () => onSelected(subject.conversationId),
            child: Container(
              padding: const EdgeInsets.symmetric(horizontal: 14),
              alignment: Alignment.center,
              decoration: BoxDecoration(
                color: selected
                    ? AppColors.ctaStart
                    : Colors.white.withValues(alpha: 0.08),
                borderRadius: BorderRadius.circular(18),
              ),
              child: Text(
                subject.name,
                style: TextStyle(
                  fontSize: 13,
                  fontWeight: FontWeight.w600,
                  color: selected
                      ? Colors.white
                      : AppColors.onBackgroundSecondary
                          .withValues(alpha: 0.85),
                ),
              ),
            ),
          );
        },
      ),
    );
  }
}
```

(c) `my_report_screen.dart`——build 內加 watch，並替換 76-80 行 HeatTrendChart 區塊：

```dart
    // 案2：對象選擇器＋單對象熱度序列（averageScore/scoreDelta 維持
    // Conversation 邏輯不動，只換 trendPoints 資料源）。
    final subjects = ref.watch(analysisSubjectsProvider);
    final selectedSubject = ref.watch(selectedReportSubjectProvider) ??
        (subjects.isEmpty ? null : subjects.first.conversationId);
    final subjectPoints = selectedSubject == null
        ? const <HeatTrendPoint>[]
        : ref.watch(subjectHeatTrendProvider(selectedSubject));
```

```dart
          if (subjects.isNotEmpty) ...[
            ReportSubjectSelector(
              subjects: subjects,
              selectedConversationId: selectedSubject,
              onSelected: (id) =>
                  ref.read(selectedReportSubjectProvider.notifier).state = id,
            ),
            const SizedBox(height: 12),
          ],
          HeatTrendChart(
            // 空狀態拍板：<2 筆畫不出趨勢 → 空清單走引導文案。
            trendPoints: subjectPoints.length >= 2
                ? subjectPoints
                : const <HeatTrendPoint>[],
            averageScore: report.averageScore,
            scoreDelta: report.scoreDelta,
            emptyMessage: '再多分析幾次，就能看到這位對象的熱度變化',
          ),
```

（import 加 `report_models.dart`、`report_subject_selector.dart`；`HeatTrendPoint` 已隨 report_models 進來。）

### Step 4: 跑測試確認過

```bash
flutter test test/widget/features/report/
```

預期：全綠。

### Step 5: Commit

```bash
git add lib/features/report/presentation/widgets/report_subject_selector.dart \
        lib/features/report/presentation/widgets/heat_trend_chart.dart \
        lib/features/report/presentation/screens/my_report_screen.dart \
        test/widget/features/report/my_report_screen_test.dart
git commit -m "案2批2 對象選擇器 chip 列：熱度卡改單對象事件序列＋<2 筆引導文案"
git push
```

---

## Task 9：練習溫度成長曲線卡（第二張卡）

**為什麼**：practice 事件的 temperatureScore 對 createdAt 畫**全域混排**時間序列（跨對象看斜率＝玩家本人的升溫能力成長）。全空或 <2 筆 → 引導文案不畫圖。

**Files:**
- Create: `lib/features/report/presentation/widgets/practice_temperature_chart.dart`（照 `heat_trend_chart.dart` 的日期 x 軸模式，簡化 header）
- Modify: `lib/features/report/presentation/screens/my_report_screen.dart`（HeatTrendChart 之後插卡）
- Test: `test/widget/features/report/practice_temperature_chart_test.dart`（新檔）＋ `my_report_screen_test.dart` 補兩個斷言

### Step 1: 寫失敗測試

```dart
// test/widget/features/report/practice_temperature_chart_test.dart
import 'package:fl_chart/fl_chart.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:vibesync/features/report/domain/entities/report_models.dart';
import 'package:vibesync/features/report/presentation/widgets/practice_temperature_chart.dart';

Future<void> _pump(WidgetTester tester, List<HeatTrendPoint> points) async {
  await tester.pumpWidget(MaterialApp(
    home: Scaffold(
      body: SingleChildScrollView(
        child: PracticeTemperatureChart(points: points),
      ),
    ),
  ));
  await tester.pumpAndSettle(); // 鐵則：動畫必收斂
}

void main() {
  testWidgets('≥2 點 → 畫線圖、x 用距首點天數', (tester) async {
    await _pump(tester, [
      HeatTrendPoint(date: DateTime(2026, 6, 1), score: 28, conversationName: ''),
      HeatTrendPoint(date: DateTime(2026, 6, 4), score: 45, conversationName: ''),
    ]);

    expect(find.text('練習溫度成長'), findsOneWidget);
    final chart = tester.widget<LineChart>(find.byType(LineChart));
    expect(
      chart.data.lineBarsData.single.spots.map((s) => s.x),
      [0.0, 3.0],
    );
  });

  testWidgets('<2 點 → 引導文案、不畫圖', (tester) async {
    await _pump(tester, [
      HeatTrendPoint(date: DateTime(2026, 6, 1), score: 28, conversationName: ''),
    ]);

    expect(find.byType(LineChart), findsNothing);
    expect(
      find.text('多完成幾場新手模式練習，這裡會畫出你的升溫能力成長曲線'),
      findsOneWidget,
    );
  });

  testWidgets('空清單 → 引導文案', (tester) async {
    await _pump(tester, const []);
    expect(find.byType(LineChart), findsNothing);
  });
}
```

`my_report_screen_test.dart` 的「案2」group 加：

```dart
    testWidgets('practice 事件 ≥2 → 練習溫度卡出現在報告區', (tester) async {
      await _pumpReportScreen(
        tester,
        subscription: const SubscriptionState(
          tier: SubscriptionTierHelper.starter,
        ),
        report: _paidReport(),
        partners: [_partner('p1', 'Vivi')],
        historyEvents: [
          AnalysisHistoryEvent.practice(
            id: 'p1',
            createdAt: DateTime(2026, 6, 1),
            profileId: 'practice_girl_001',
            roundIndex: 1,
            temperatureScore: 28,
          ),
          AnalysisHistoryEvent.practice(
            id: 'p2',
            createdAt: DateTime(2026, 6, 4),
            profileId: 'practice_girl_002',
            roundIndex: 1,
            temperatureScore: 45,
          ),
        ],
      );

      expect(find.byType(PracticeTemperatureChart), findsOneWidget);
      expect(find.text('練習溫度成長'), findsOneWidget);
    });
```

（import `practice_temperature_chart.dart`。）

### Step 2: 跑測試確認失敗

```bash
flutter test test/widget/features/report/practice_temperature_chart_test.dart
```

預期：編譯失敗（widget 不存在）。

### Step 3: 最小實作

```dart
// lib/features/report/presentation/widgets/practice_temperature_chart.dart
import 'package:fl_chart/fl_chart.dart';
import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

import '../../../../core/theme/app_colors.dart';
import '../../../../shared/widgets/brand/brand_kit.dart';
import '../../domain/entities/report_models.dart';

/// 案2：練習溫度成長曲線——practice 歷史事件的 temperatureScore 對
/// createdAt 的全域時間序列（刻意不分對象混排：練習溫度量的是玩家本人
/// 的開場→升溫能力，跨對象看斜率才是成長曲線）。<2 點顯示引導文案。
class PracticeTemperatureChart extends StatelessWidget {
  final List<HeatTrendPoint> points;

  const PracticeTemperatureChart({super.key, required this.points});

  @override
  Widget build(BuildContext context) {
    return BrandSurfaceCard(
      padding: const EdgeInsets.all(16),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            '練習溫度成長',
            style: TextStyle(
              fontSize: 12,
              color: AppColors.onBackgroundSecondary.withValues(alpha: 0.78),
            ),
          ),
          const SizedBox(height: 16),
          points.length < 2 ? _buildEmptyState() : _buildChart(),
        ],
      ),
    );
  }

  Widget _buildEmptyState() {
    return SizedBox(
      height: 140,
      child: Center(
        child: Text(
          '多完成幾場新手模式練習，這裡會畫出你的升溫能力成長曲線',
          textAlign: TextAlign.center,
          style: TextStyle(
            fontSize: 14,
            color: AppColors.onBackgroundSecondary.withValues(alpha: 0.70),
          ),
        ),
      ),
    );
  }

  Widget _buildChart() {
    final sorted = List<HeatTrendPoint>.from(points)
      ..sort((a, b) => a.date.compareTo(b.date));
    final firstDate = sorted.first.date;
    double xOf(DateTime date) =>
        date.difference(firstDate).inMinutes / (24 * 60.0);
    final spots = [
      for (final point in sorted)
        FlSpot(xOf(point.date), point.score.toDouble()),
    ];
    final maxX = spots.last.x <= 0 ? 1.0 : spots.last.x;
    final dateFormat = DateFormat('M/dd');
    final bottomInterval = maxX <= 4 ? 1.0 : (maxX / 4).ceilToDouble();

    return SizedBox(
      height: 160,
      child: LineChart(
        LineChartData(
          minX: 0,
          maxX: maxX,
          minY: 0,
          maxY: 100,
          clipData: const FlClipData.all(),
          gridData: FlGridData(
            show: true,
            drawVerticalLine: false,
            horizontalInterval: 25,
            getDrawingHorizontalLine: (value) => FlLine(
              color: Colors.white.withValues(alpha: 0.10),
              strokeWidth: 0.8,
            ),
          ),
          titlesData: FlTitlesData(
            topTitles:
                const AxisTitles(sideTitles: SideTitles(showTitles: false)),
            rightTitles:
                const AxisTitles(sideTitles: SideTitles(showTitles: false)),
            leftTitles: AxisTitles(
              sideTitles: SideTitles(
                showTitles: true,
                reservedSize: 32,
                interval: 25,
                getTitlesWidget: (value, meta) {
                  if (value % 25 != 0) return const SizedBox.shrink();
                  return Text(
                    value.toInt().toString(),
                    style: TextStyle(
                      fontSize: 10,
                      color: AppColors.onBackgroundSecondary
                          .withValues(alpha: 0.70),
                    ),
                  );
                },
              ),
            ),
            bottomTitles: AxisTitles(
              sideTitles: SideTitles(
                showTitles: true,
                reservedSize: 28,
                interval: bottomInterval,
                getTitlesWidget: (value, meta) {
                  if (value < 0 || value > maxX) {
                    return const SizedBox.shrink();
                  }
                  final date = firstDate
                      .add(Duration(minutes: (value * 24 * 60).round()));
                  return Padding(
                    padding: const EdgeInsets.only(top: 6),
                    child: Text(
                      dateFormat.format(date),
                      style: TextStyle(
                        fontSize: 10,
                        color: AppColors.onBackgroundSecondary
                            .withValues(alpha: 0.70),
                      ),
                    ),
                  );
                },
              ),
            ),
          ),
          borderData: FlBorderData(show: false),
          lineBarsData: [
            LineChartBarData(
              spots: spots,
              isCurved: true,
              curveSmoothness: 0.3,
              color: AppColors.ctaStart,
              barWidth: 2.5,
              isStrokeCapRound: true,
              dotData: FlDotData(
                show: true,
                getDotPainter: (spot, percent, bar, index) =>
                    FlDotCirclePainter(
                  radius: 4,
                  color: Colors.white,
                  strokeWidth: 2,
                  strokeColor: AppColors.ctaStart,
                ),
              ),
              belowBarData: BarAreaData(
                show: true,
                color: AppColors.ctaStart.withValues(alpha: 0.1),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

`my_report_screen.dart`：build 加 `final practicePoints = ref.watch(practiceTemperatureTrendProvider);`，HeatTrendChart 之後插入：

```dart
          const SizedBox(height: 16),
          PracticeTemperatureChart(points: practicePoints),
```

（放在付費非空分支內、ConversationComparisonChart 之前。已知取捨：totalConversations==0 的整頁空狀態下不顯示練習卡——「報告頁其他區塊不動」拍板，不改整頁 gating。import 加 `practice_temperature_chart.dart`。）

### Step 4: 跑測試確認過

```bash
flutter test test/widget/features/report/
```

預期：全綠。

### Step 5: Commit

```bash
git add lib/features/report/presentation/widgets/practice_temperature_chart.dart \
        lib/features/report/presentation/screens/my_report_screen.dart \
        test/widget/features/report/practice_temperature_chart_test.dart \
        test/widget/features/report/my_report_screen_test.dart
git commit -m "案2批2 練習溫度成長曲線卡：practice 事件全域時間序列＋空狀態引導"
git push
```

---

## Task 10：全測試套件跑綠＋收尾

**Files:** 無新檔（只修 fallout）

### Step 1-4: 驗證

```bash
flutter test
```

已知事實：repo 測試套件在 Linux/WSL 歷史上有 stale rot（見 memory：86 stale rot＋boy-scout 策略）。判定標準：

1. 先在**本案改動前的 main**（`git stash` 或對照 CI 最近一次 main run）確認基線失敗清單。
2. 本案後的失敗集合**必須是基線的子集**——任何新增失敗都是本案 fallout，必修。
3. 本案觸碰過的測試檔（Task 1-9 全部列過）必須 100% 綠。

另跑 analyzer 確認零新 warning：

```bash
flutter analyze lib/features/analysis_history lib/features/report \
  lib/core/services/storage_service.dart lib/core/constants/app_constants.dart
```

### Step 5: Commit（若有 fallout 修復才需要）

```bash
git add <只加實際修過的檔案>
git commit -m "案2批2 收尾：全套測試對基線跑綠"
git push
```

**完成後**：本案動了 Hive 註冊（高風險區）→ 照專案規則呼叫 codex:rescue 雙審（範圍：Task 1-5 的 Hive/entity/hook diff 為主），拿到 verdict 才可宣稱 dogfood safe。真機體感驗證留 Eric dogfood（與案1批3/批4 同車）。

---

## 明確不做（設計拍板，執行時不得自行加回）

- 不回填舊資料（拿不到真時間點，硬造失真）。
- 不動 Edge schema、訂閱、quota、RevenueCat 任何一行。
- errorPattern 枚舉化＝獨立成案，不入本案。
- `familiarityScore` 不畫第二條線（YAGNI）。
- `generateReport` 的 averageScore/scoreDelta/comparisons/stageDistributions 邏輯不動。
- analyze hook 不另做冪等（既有 gate 已保證）；practice hook 不做去重（每次 _persist 一點＝溫度隨回合演進的曲線，是 feature 不是 bug）。
