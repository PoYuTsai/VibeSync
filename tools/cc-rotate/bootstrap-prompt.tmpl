🔄 **ROTATION BOOTSTRAP — YOUR VERY FIRST ACTIONS**

You just rotated from a previous Claude Code session that intentionally ended at {{TS}}.
This is **NOT** a cold start. The user is on mobile Discord and is **waiting for your reply**.

Discord frontline behavior:
- Eric and Bruce may both report issues in the same channel. If both spoke before your reply, explicitly address both by name or mention in one response.
- Quote or name the key message you are answering when there is any chance of ambiguity; do not silently answer only the latest speaker.
- If a requirement is unclear or has product / billing / data-risk tradeoffs, ask a concise clarifying question before file edits. Read-only investigation is allowed; write operations need a clear task.
- Discord can reliably carry text and screenshots. If a report depends on video, ask for key screenshots, timestamps, exact steps, expected result, and actual result before making product/code judgments.

- Previous session HEAD: `{{OLD_HEAD}}`
- User initiated rotation via Discord channel: `{{DISCORD_CHANNEL_ID}}`
- Handoff written by previous session: `{{HANDOFF_PATH}}`

═══════════════════════════════════════════════════════════════════════
**MANDATORY first-turn protocol — execute IN ORDER. Parallelize 1a-1d.**
═══════════════════════════════════════════════════════════════════════

**Step 1 — Parallel context load** (one message, multiple Read tool calls):

- 1a. Read `{{AGENTS_MD_PATH}}` (full)
- 1b. Read `{{SHARED_RULES_PATH}}` (full)
- 1c. Read `{{SNAPSHOT_PATH}}` (full)
- 1d. Read the newest OPEN item in `{{QUEUE_PATH}}` (or the first 220 lines if a scoped read is easier)
- Treat the top `Current Focus Guardrail` in snapshot as authoritative over memory index. Do not revive older roadmap items unless Eric explicitly asks.

**Step 2 — Read handoff** (single Read, full file):

- Read `{{HANDOFF_PATH}}`

**Step 3 — Verify repo state** (single Bash, two commands chained):

- Run `git log --oneline -12 && git status --short --branch` via Bash.
- Confirm HEAD still equals `{{OLD_HEAD}}` — no concurrent commits should have happened during rotation. If mismatch, flag in step 5 reply.
- Use the latest 12 commits to understand the last few rounds before taking any task. If memory index conflicts with these commits or snapshot, trust commits + snapshot.

**Step 4 — Optional bug-log peek** (only if file ≤ 200 lines):

- Check `{{BUG_LOG_PATH}}` line count via `wc -l`. If ≤ 200, grep last 5 entry headings to seed recent bug awareness. If > 200, **skip** — handoff already covers what matters.

**Step 5 — Reply to Discord** (use the Discord reply tool, channel `{{DISCORD_CHANNEL_ID}}`):

Required content:
- Status line: `✅ New session ready.`
- HEAD: 7-char hash from step 3
- Handoff summary: N open loops with one-line each (from step 2)
- Live queue: one-line newest OPEN item / queued dogfood intake status (from step 1d)
- (If warnings present, see below) one line: `⚠️ N warnings from previous session: <comma-separated codes>`
- Closing line: `Awaiting next task.`

Keep it phone-screen friendly (≤ 8 lines total, no wall-of-text).

**Step 6 — Cleanup** (Bash):

- Run `rm "{{BOOTSTRAP_JSON_PATH}}"` to delete the bootstrap manifest. Skipping this causes R3 stale-bootstrap on next session start.

═══════════════════════════════════════════════════════════════════════

**Warnings carried over from previous session:**
{{WARNINGS_BLOCK}}

═══════════════════════════════════════════════════════════════════════

**Hard rules during bootstrap:**

- ❌ Do NOT start any new work until Steps 1-6 complete.
- ❌ Do NOT trust memory index over snapshot / git log. Snapshot + latest commits define current product track.
- ❌ Do NOT skip Step 5 — the user is on mobile waiting for confirmation.
- ❌ Do NOT skip Step 6 — leftover bootstrap.json poisons next session.
- ❌ Do NOT echo warnings verbatim in Step 5 — summarize codes only (e.g. "W1, W2").
- ✅ If any step fails, reply to Discord channel `{{DISCORD_CHANNEL_ID}}` with the specific error and `❌ Bootstrap failed at step <N>: <reason>`, then stop.

After Steps 1-6 complete, proceed as a normal Claude Code session for VibeSync. Your context is now seeded with: AGENTS rules · shared agent rules · current product snapshot · full handoff · current repo state. You should know exactly where the previous session left off.
