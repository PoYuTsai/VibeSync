# ROTATION BOOTSTRAP - READ BEFORE DOING ANY WORK

You just rotated from a previous Claude Code Discord session at {{TS}}.
This is not a cold start. Eric may be on mobile and waiting in Discord.

Important stale-context guard:
- Claude persisted output may show old archived roadmap memories.
- Do not trust old memory until you load the files below.
- Coach 1:1 is already shipped into dogfood and is part of the core product.
- Current default track is TestFlight dogfood / App Review readiness stabilization.

Previous session HEAD: `{{OLD_HEAD}}`
Discord channel: `{{DISCORD_CHANNEL_ID}}`
Handoff path: `{{HANDOFF_PATH}}`

## Mandatory first-turn protocol

Do these steps in order. Do not start new work until all are complete.

### Step 1 - Load project truth

Read these files first:

1. `{{AGENTS_MD_PATH}}`
2. `{{SHARED_RULES_PATH}}`
3. `{{SNAPSHOT_PATH}}`
4. newest OPEN item in `{{QUEUE_PATH}}` (or first 220 lines if easier)

Treat `docs/snapshot.md` plus `git log --oneline -15` as authoritative over memory.

### Step 2 - Read handoff

Read `{{HANDOFF_PATH}}` in full.

### Step 3 - Verify repo state

Run:

```bash
git log --oneline -15
git status --short --branch
```

Confirm whether HEAD still matches `{{OLD_HEAD}}`.
If it does not match, mention the mismatch in the Discord ready reply.

### Step 4 - Optional recent bug awareness

Check `{{BUG_LOG_PATH}}` line count.
If it is 200 lines or fewer, skim the newest 5 headings.
If it is longer, skip it and rely on snapshot + handoff + queue.

### Step 5 - Reply to Discord

Reply to channel `{{DISCORD_CHANNEL_ID}}` with 8 lines or fewer:

- `New session ready.`
- `HEAD: <7-char hash>`
- `Open loops: <N> - <one-line summary>`
- `Queue: <newest OPEN item or none>`
- warnings summary if any
- `Awaiting next task.`

If Eric and Bruce both have unresolved messages, explicitly acknowledge both.

### Step 6 - Cleanup

Run:

```bash
rm "{{BOOTSTRAP_JSON_PATH}}"
```

If cleanup fails, tell Discord and stop.

## Warnings carried over

{{WARNINGS_BLOCK}}

## Hard rules

- Do not trust old chat memory over snapshot / git log / handoff.
- Do not revive old roadmap tracks unless Eric asks.
- Do not claim a build is safe without saying what was tested.
- High-risk hotfixes need Codex read-only review before dogfood approval.
- When reporting fixes/reviews to Discord, use the closeout format in `docs/shared-agent-rules.md`.
- Keep Discord replies short and specific.
