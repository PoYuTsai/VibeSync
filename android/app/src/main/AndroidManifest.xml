<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- AND-04 fail-closed 備份政策：token／加密 Hive／聊天與圖片等敏感
         資料一律不進雲端備份與換機轉移。allowBackup=false 為主閘，兩份
         rules 檔是防未來誤開 allowBackup 的第二道欄（全域 exclude）。
         OEM 轉移工具是否尊重此設定不能只看 XML，見
         docs/android-backup-restore-checklist.md 的實機驗證清單。 -->
    <application
        android:label="VibeSync"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="s">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Email 深連結契約（signup 確認／resend／密碼重設）：
                 com.poyutsai.vibesync://login-callback 唯一擁有者是
                 MainActivity（singleTop：冷啟走 launch intent、暖啟走
                 onNewIntent），由 app_links／supabase_flutter 接手處理。
                 OAuth 走 oauth-callback（下方 CallbackActivity），兩條路
                 host 不同不會撞 activity 選擇器，密碼重設連結也不會被
                 CallbackActivity 吞掉。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="com.poyutsai.vibesync"
                    android:host="login-callback" />
            </intent-filter>
        </activity>
        <!-- flutter_web_auth_2 4.1.0 的 OAuth callback 契約：
             com.poyutsai.vibesync://oauth-callback 唯一擁有者是
             CallbackActivity。MainActivity 不得宣告同 host，否則同 App
             兩個 activity 都匹配會跳選擇器，OAuth 回不了 App。
             此 URI 需在 Supabase Auth Redirect URLs allowlist，
             見 docs/integrations/auth.md。 -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="com.poyutsai.vibesync"
                    android:host="oauth-callback" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications：排程通知到期時的顯示 receiver。
             plugin 18.x 的 library manifest 不含此宣告，必須 app 端手動加，
             否則 48h 跟進通知到期會靜默不顯示（Codex 案4 F1）。
             boot 持久化刻意不做，故不加 ScheduledNotificationBootReceiver /
             RECEIVE_BOOT_COMPLETED（最小化送審面權限）。 -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
