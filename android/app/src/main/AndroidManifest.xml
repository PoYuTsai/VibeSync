<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- AND-04 fail-closed 備份政策：token／加密 Hive／聊天與圖片等敏感
         資料一律不進雲端備份與換機轉移。allowBackup=false 為主閘，兩份
         rules 檔是防未來誤開 allowBackup 的第二道欄（全域 exclude）。
         OEM 轉移工具是否尊重此設定不能只看 XML，見
         docs/android-backup-restore-checklist.md 的實機驗證清單。 -->
    <application
        android:label="VibeSync"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="s">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 凍結深連結契約（OAuth＋email 共用）：
             com.poyutsai.vibesync://login-callback 唯一 manifest 擁有者是
             AuthCallbackDispatcherActivity——OAuth 進行中（flutter_web_auth_2
             有 live callback）交回 plugin，否則原樣轉送 singleTop
             MainActivity（冷啟 onCreate、暖啟 onNewIntent），由
             app_links／supabase_flutter 接手。單一擁有者＝不會跳
             activity 選擇器；分流細節見 AuthCallbackDispatcherActivity。
             不得再宣告 flutter_web_auth_2 CallbackActivity 或第二個
             同 scheme intent-filter，否則會撞選擇器。

             taskAffinity=""（Main 與 dispatcher 皆設）刻意偏離 pinned
             flutter_web_auth_2 4.1.0 README「移除 taskAffinity」的建議：
             空 affinity 讓惡意 app 無法用同名 affinity 併進我們的 task
             （task-hijacking／StrandHogg 緩解，API<30 仍可被利用）。
             轉送仍正確：NEW_TASK 找既有 task 時，affinity 比對不到會再用
             task root component 比對（AOSP Task#isSameIntentFilter），
             MainActivity 是 launcher task 的 root，必回到既有 task 的
             onNewIntent、不疊第二實例。可執行證據：install-smoke.sh 的
             「既有 Main→callback→dumpsys 單實例／單 task」步驟（API 24＋36）。 -->
        <activity
            android:name=".AuthCallbackDispatcherActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="com.poyutsai.vibesync"
                    android:host="login-callback" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications：排程通知到期時的顯示 receiver。
             plugin 18.x 的 library manifest 不含此宣告，必須 app 端手動加，
             否則 48h 跟進通知到期會靜默不顯示（Codex 案4 F1）。
             boot 持久化刻意不做，故不加 ScheduledNotificationBootReceiver /
             RECEIVE_BOOT_COMPLETED（最小化送審面權限）。 -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
